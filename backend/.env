# Server
PORT=5000

# MongoDB — replace with your Atlas URI or keep localhost
MONGODB_URI=mongodb://localhost:27017/cacheflow

# Redis (optional — lea ve as-is if not running Redis locally)
REDIS_URL=redis://localhost:6379
